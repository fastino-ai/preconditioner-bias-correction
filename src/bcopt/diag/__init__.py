"""Diagnostic probes (development utilities).

  probe_adamw       — per-step shadow-AdamW state probe.
  train_hooks       — generic per-step hooks used by the diag pipelines.
  update_alignment  — measure cosine alignment between BC and std updates.
  sym_alignment     — analogous probe for the symmetrized BC variant.
"""
