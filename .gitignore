# Python bytecode
__pycache__/
*.pyc
*.pyo

# Build / packaging artifacts
build/
dist/
*.egg-info/

# Editor/OS junk
.DS_Store
*.swp
*.swo

# Runtime state
*.log.tmp
*.json.tmp

# Local data dumps (kept on disk, not in git). Scope to repo root so we
# don't accidentally ignore src/bcopt/data/ (the data-prep Python module).
/data/

# Experiment outputs (logs, histories, checkpoints, plots) - ~200GB locally
/runs/

# Paper sources (not part of the released code)
Bias_Corrected_Preconditioned_Optimization_for_Language_Model_Training/

# Misc temp / scratch
*.tmp
/tmp/auto_chain_noisy.log

# Virtualenvs
.venv/
venv/

